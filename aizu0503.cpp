#include <iostream>

using namespace std;

int main()
{
    while(true)
    {
        int num;
        int limit;
        cin >> num >> limit;
        if(num == 0 && limit == 0)

        for(int i = 0; i < num; i++)
        {
            cin >>
        }

    }
}